<Project Sdk="Microsoft.NET.Sdk.Web">

  <PropertyGroup>
    <TargetFramework>net8.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
  </PropertyGroup>

</Project>
<!--
 Optional: If you want to keep local /lib assets managed by LibMan, uncomment this target
 and ensure LibMan CLI is available on the build agent.
 <Target Name="LibManRestore" BeforeTargets="Build">
   <Exec Command="libman restore" />
 </Target>
-->
