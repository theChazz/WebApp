name: Build and Deploy to Azure

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: '9.0.x'

    - name: Restore dependencies
      run: dotnet restore ./WebApp/WebApp.csproj

    - name: Build
      run: dotnet build ./WebApp/WebApp.csproj --configuration Release --no-restore

    - name: Test
      run: dotnet test ./WebApp/WebApp.csproj --configuration Release --no-build --verbosity normal

    - name: Publish
      run: dotnet publish ./WebApp/WebApp.csproj --configuration Release --output ./publish --no-build

    - name: Login to Azure
      uses: azure/login@v1
      with:
        creds: ${{ secrets.AZURE_CREDENTIALS }}

    - name: Deploy to Azure Web App
      uses: azure/webapps-deploy@v3
      with:
        app-name: 'LMS-WEBAPP'
        slot-name: 'production'
        package: ./publish